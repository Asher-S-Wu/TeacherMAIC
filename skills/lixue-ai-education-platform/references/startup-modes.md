# 启动与部署说明

李雪 AI 教育平台以 Vercel 部署为准，本地没有固定运行环境。

## 默认方式

- 线上访问：https://teacher.li-xue.com/
- 部署平台：Vercel Pro

## 操作边界

- 未经用户同意，不运行 `pip`、`npm`、`npx` 等命令。
- 未经用户同意，不创建说明文件、示例环境变量文件、测试文件或测试代码。
- 需要检查线上状态时，可访问 `/api/health`。
